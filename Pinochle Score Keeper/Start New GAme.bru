meta {
  name: Start New GAme
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/api/games
  body: json
  auth: none
}

body:json {
  {
    "dealer": "South"
  }
}

vars:post-response {
  game_id: 
}

script:post-response {
  const response = res.getBody();
  
  const extractedValue = response.game_id
  
  bru.setVar("game_id", extractedValue);
}
